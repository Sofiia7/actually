# Node / build
node_modules/
dist/
.vite/
*.log

# Local env / secrets
.env
.env.local
*.local

# Build artifacts (we ship via CWS / GitHub Releases, not via repo)
*.zip
*.tar.gz

# Cloudflare Worker
worker/.wrangler/
worker/.dev.vars

# market-cache-builder --dry-run output (local inspection only, never committed)
packages/market-cache-builder/market-cache-blob.json
# market-cache-builder's local embedding model cache (env.cacheDir in build.ts)
packages/market-cache-builder/.cache/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Claude / agent session data
.claude/
.cursor/
.deploy-secrets.local
