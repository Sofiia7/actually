node_modules/
dist/
.vite/
*.log
.DS_Store

# Build / packaging artifacts — never commit
*.zip
*.tar.gz
*.crx

# Cloudflare Worker
worker/.wrangler/
worker/.dev.vars

# Local env / secrets
.env
.env.local
*.local

# Bundled model + WASM runtime (populated by `npm run models:fetch`, see
# scripts/fetch-model.mjs). ~33MB — reproducibly fetched at build time
# instead of committed, same rationale as node_modules/.
public/models/
public/onnx/

# IDE
.idea/
.vscode/
